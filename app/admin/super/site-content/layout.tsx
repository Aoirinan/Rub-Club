import type { ReactNode } from "react";

/** Auth + hub nav live on parent `app/admin/super/layout.tsx`. */
export default function SiteContentLayout({ children }: { children: ReactNode }) {
  return children;
}
