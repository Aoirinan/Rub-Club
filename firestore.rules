rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // All reads/writes go through Next.js API routes using the Admin SDK.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
